version: 1

before:
  hooks:
    - go mod tidy
    - go generate ./...
    - tar -cvzf dashboards-{{ .Version }}.tar.gz dashboards/* > /dev/null 2>&1

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
      - freebsd
      - openbsd
      - netbsd
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X 'main.Version={{.Version}}'
      - -X 'main.VersionHash={{.ShortCommit}}'
      - -X 'main.VersionDate={{.Date}}'

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}-
      {{- .Version }}.
      {{- .Os }}-
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - CHANGELOG.md
      - README.md

release:
  extra_files:
    - glob: dashboards-*.tar.gz

checksum:
  name_template: 'sha256sums.txt'
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
